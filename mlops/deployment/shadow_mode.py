"""Shadow mode for silent evaluation"""

class ShadowMode:
    """Run new model silently for eval"""
    
    async def run_shadow(self, query: str, new_model: str) -> dict:
        """Run new model in shadow mode"""
        pass
